source 'https://rubygems.org'

# Webserver for running the site locally
gem 'asdf'

# Gems for making changes to the site locally.
group :development do
  gem 'guard'
  gem 'guard-compat'
  gem 'guard-haml'

  gem 'kramdown'
end
